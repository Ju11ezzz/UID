<Window x:Class="TextEditor_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Текстовий редактор"
        Height="600"
        Width="900"
        MinHeight="500"
        MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Меню -->
        <Menu Background="White" FontFamily="Segoe UI" FontSize="12" Height="30">
            <MenuItem Header="Файл">
                <MenuItem Header="Новий" Click="NewFile_Click"/>
                <MenuItem Header="Відкрити" Click="OpenFile_Click"/>
                <MenuItem Header="Зберегти" Click="SaveFile_Click"/>
                <Separator/>
                <MenuItem Header="Вихід" Click="Exit_Click"/>
            </MenuItem>
        </Menu>

        <!-- Основне текстове поле -->
        <TextBox x:Name="textBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 Background="White"
                 Foreground="Black"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Padding="10"
                 TextChanged="textBox_TextChanged"/>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2" Background="#E0E0E0" FontFamily="Segoe UI" FontSize="12">
            <TextBlock x:Name="statusText" Text="Готово" />
        </StatusBar>
    </Grid>
</Window>
